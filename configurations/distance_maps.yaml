hydra:
  run:
    dir: outputs/${directory.culture}/${directory.file_name}/ch_${params.channels}_mix_${params.mixup}
  sweep:
    dir: outputs/${directory.culture}
    subdir: ${directory.file_name}/ch_${params.channels}_mix_${params.mixup}
directory:
  condition: 230824_Homer1_ATTO643_Bassoon_CF568_CaV21_AF488_pansCF405
  culture: Culture 1
  root_dir: D:\Daten\Stefan\Revision\Frotiers Review\\${directory.condition}\\${directory.culture}\\Best
  file_name: Experiment-8095-Airyscan Processing-02-3
  save_dir: results
  save_name: auto
params:
  channels: [0,1]
  mixup: False
  z_project: False
  px_size: 0.0411769
transform:
  type: affine
  parameter_file: static/affine_noscaling_parameters.txt
